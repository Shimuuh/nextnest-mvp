# ============================================================
# requirements.txt — Python Dependencies for HopeLink AI Engine
# Install all dependencies with:
#   pip install -r requirements.txt
# ============================================================

# ------------------------------------------------------------
# SERVER
# FastAPI — web framework for the AI engine API
# uvicorn — ASGI server that runs FastAPI
# ------------------------------------------------------------
fastapi==0.115.0
uvicorn==0.30.6

# ------------------------------------------------------------
# DATA VALIDATION
# pydantic — data models and validation (Intent, UserRequest etc.)
# ------------------------------------------------------------
pydantic==2.9.2

# ------------------------------------------------------------
# ENVIRONMENT VARIABLES
# python-dotenv — loads .env file into os.environ
# ------------------------------------------------------------
python-dotenv==1.0.1

# ------------------------------------------------------------
# HTTP CLIENT
# httpx — async HTTP client used by read_tools and write_tools
# to call the Node.js backend API
# ------------------------------------------------------------
httpx==0.27.2

# ------------------------------------------------------------
# LLM PROVIDERS
# Install both — system uses whichever key is set in .env
# anthropic — Claude (recommended)
# openai    — GPT-4 (alternative)
# ------------------------------------------------------------
anthropic==0.34.2
openai==1.51.0

# ------------------------------------------------------------
# UTILITIES
# ------------------------------------------------------------
python-multipart==0.0.12    # needed for FastAPI form data